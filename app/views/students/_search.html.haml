=form_tag students_search_path(course), :method => "POST", :class => "form-inline" do |f|
  %input{type: "text", class: "form-control", placeholder: "Search by Username", name: :username, value: username}
  =submit_tag "Search for students", :class => 'btn btn-primary'  
