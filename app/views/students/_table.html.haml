-if students.count > 0
  .row
    .col-md-10.col-md-offset-1
      %table.table.table-striped
        %thead
          %tr
            %th Username
            %th Ungraded Submissions
            %th Student Info
        %tbody
          -students.each do |student|
            %tr
              %td= student.username
              %td= student.submissions.ungraded.count
              %td= link_to("View Student Info", student_path(student), :class=>"btn btn-success")        
-else
  %p You have no students for this course